runtime: python311

env_variables:
  SECRET_KEY: "change-this-in-production"
  # ANTHROPIC_API_KEY: set via Secret Manager in production

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto

automatic_scaling:
  min_instances: 0
  max_instances: 10
